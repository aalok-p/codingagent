## Codebase Patterns
- Analytics queries use SQLAlchemy `func.sum`, `func.count`, `func.coalesce` for aggregation scoped to `seller_id`
- Frontend is plain React 19 + Vite + React Router v7 (JSX files, no TypeScript, no Tailwind)
- API modules use separate Axios instances with localStorage-based JWT interceptor
- Toast system via ToastContext + ToastProvider; use `const toast = useToast()` and call `toast(msg)` or `toast(msg, 'error')`
- Styling is custom CSS in App.css with CSS custom properties and `@media (prefers-color-scheme: dark)` for dark mode
- Auth state in localStorage keys: `token`, `userId`, `role`
- Protected routes via `<ProtectedRoute>` wrapper in App.jsx reading localStorage
- All CSS class patterns: `.btn-primary`, `.btn-small`, `.btn-secondary`, `.btn-icon`, `.modal-overlay`, `.modal`, `.modal-actions`, `.form-group`, `.empty-state`, `.page-loading`, `stock-badge`/`stock-high`/`stock-medium`/`stock-low`
- Backend orders already return items with `product_name` in GET /orders response
- New routers need to be registered in both the import and include_router sections of main.py + schemas exported in schemas/__init__.py
- Complaints API endpoints: POST /complaints (buyer), GET /complaints/seller (seller), PATCH /complaints/{id}/resolve (seller)

## [Sat Jun 20 2026] - US-010
- Implemented buyer order history page with expandable order cards
- Each order shows: order ID, date, item count, total, status badge
- Click to expand shows order items with product name, qty, unit price, subtotal
- File Complaint button on each order item opens a complaint form modal
- Added "My Orders" navigation link in buyer header
- Added getOrders() and getOrder() to orders API module
- Added /buyer/orders route in App.jsx
- Added CSS for orders page, status badges, complaint form
- **Learnings for future iterations:**
  - Status badge colors need 5 variants (pending, confirmed, shipped, delivered, cancelled) with dark mode support
  - The orders API returns items with `product_name` field directly (not nested product object)
  - Buyer header actions should use a flex container for multiple action buttons (cart + nav links)
  - ESLint + Vite build passed without issues — no TypeScript checking needed for this project
  - Complaint form modal is placeholder UI — will need to POST to /complaints when backend endpoint is built (US-013)
---
## [Sat Jun 20 2026] - US-011
- Implemented GET /seller/analytics endpoint returning total_products_listed, total_units_sold, total_revenue, low_stock_products
- Created app/schemas/seller.py with SellerAnalyticsResponse and LowStockProduct schemas
- Created app/routers/seller.py with analytics query using SQLAlchemy aggregation (func.sum, func.count, func.coalesce)
- Registered seller router in app/main.py
- **Learnings for future iterations:**
  - New routers need to be registered in both the import and include_router sections of main.py
  - Use `func.coalesce` to avoid None results from sum/count aggregations on empty tables
  - The `/seller` prefix pattern is established for seller-specific endpoints
  ---
## [Sat Jun 20 2026] - US-012
- Implemented seller analytics dashboard page with four stat cards (Products Listed, Units Sold, Revenue, Low Stock)
- Created seller API module at src/api/seller.js with getAnalytics() following existing pattern
- Created src/pages/seller/Analytics.jsx with stat cards grid and low stock products list
- Added seller navigation header (Products / Analytics tabs) reused across Products.jsx and Analytics.jsx
- Added /seller/analytics route in App.jsx
- Added CSS for stat cards, low stock list, seller header navigation with dark mode support
- **Learnings for future iterations:**
  - Seller navigation pattern is now established — both Products and Analytics pages share the same header/nav structure
  - When adding new seller pages, add the nav link in both Products.jsx and Analytics.jsx headers
  - The `seller-header` + `seller-nav` CSS classes are shared between seller pages
  - No TypeScript typecheck needed for this project (plain JSX + Vite)
   - Manual browser verification needed for UI pages
---
## [Sat Jun 20 2026] - US-013
- Implemented complaints API with 3 endpoints
- POST /complaints — buyer only; validates order_item exists, customer exists, no duplicate complaint per order item; creates complaint linked to product and customer
- GET /seller/complaints — seller only; returns complaints on seller's products with product name, customer name, message, status, created_at; ordered by newest first
- PATCH /complaints/{id}/resolve — seller only; validates complaint exists, belongs to seller's product, not already resolved; sets status to "resolved"
- Created app/schemas/complaint.py with ComplaintCreate, ComplaintResponse, SellerComplaintResponse
- Created app/routers/complaints.py with role-based access control
- Registered complaints router in app/main.py and schemas in __init__.py
- **Learnings for future iterations:**
  - Complaint model has a one-to-one relationship with order_item (uselist=False) — enforce no duplicate complaints per order_item
  - The complaints router uses /complaints prefix for buyer endpoints and /complaints/seller for seller-specific listing — consistent with the /seller prefix pattern
  - Enforce that customer must exist (i.e., user must have placed an order) before allowing complaint filing
  - New router registration requires 3 steps: create router file, add import + include_router in main.py, export schemas in __init__.py
---
## [Sat Jun 20 2026] - US-014
- Implemented complaints UI: buyer complaint submission + seller complaints panel
- Created src/api/complaints.js with fileComplaint(), getSellerComplaints(), resolveComplaint() following existing API module pattern (axios + auth interceptor)
- Wired up buyer complaint form modal in Orders.jsx to POST /complaints with error handling
- Created src/pages/seller/Complaints.jsx with complaints list showing product name, customer name, message, status badge, and Resolve button
- Added /seller/complaints route in App.jsx
- Added Complaints navigation link in Products.jsx and Analytics.jsx seller headers
- Added CSS for seller complaints panel (complaint-card, complaint-card-header, etc.)
- Build and lint pass without errors
- Manual browser verification needed for UI pages
---
